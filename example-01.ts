function sumOfTwoNumberss( firstNumber: number, secondNumber: number ): number {
    return firstNumber + secondNumber;
}

const numberAs: number = 5;
const numberBs: number = 15;

const results = sumOfTwoNumberss( numberAs, numberBs );

console.log(`The sum of values(${numberAs}, ${numberBs}) is: ${results}`);
